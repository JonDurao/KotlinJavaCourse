package BasicDifferencesWithJava.HelloWorld

/**
 *  COMPILE KOTLIN
 *  Compiler takes files of type .kt and generates bytecode as .class, java files, that'll be runned by the JVM
 **/

/**
 * IMPORTS
 * A lot of classes are imported by default, called KOTLIN STANDARD LIBRARY
 *
 */

/**
 *  FUNCTIONS OUTSIDE CLASSES -> TOP-LEVEL FUNCTION
 *  Functions can be outside classes, the compiler takes care of it
 */

fun main(args: Array<String>) {
    println("Hello World!")
}